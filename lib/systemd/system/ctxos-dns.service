[Unit]
Description=CtxOS DNS Service
After=network.target

[Service]
Type=forking
ExecStart=/usr/sbin/dnsmasq -C /etc/ctxos/dnsmasq.conf --pid-file
ExecReload=/bin/kill -HUP $MAINPID
Restart=on-failure

[Install]
WantedBy=multi-user.target
